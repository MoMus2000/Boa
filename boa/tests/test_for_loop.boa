var a = 0;
var temp;

assert_eq(temp, nil, "Expected nil but got "+ temp);

for (var b = 1; a < 10000; b = temp + b;) {
  temp = a;
  assert_eq(temp, a, "Expected temp = a but got temp = "+ t + "a = "+a);
  a = b;
  assert_eq(a, b, "Expected a == b, but got a = "+a+" b = "+b);
}
